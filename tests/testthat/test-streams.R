context("unpack from streams")

#most of these tests to be implemented using a raw in-memory connection

test_that("consume sequentially", {})
test_that("consume non-blocking", {})
test_that("consume some number", {})
test_that("consume all available", {})
test_that("max buffer size", {})
