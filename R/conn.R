
read_msg <- function(conn, ...) {
  stop("not written");
}

write_msg <- function(conn, ...) {
  stop("not written");
}

msg_stream <- function(conn, ...) {
  stop("not written");
}

read.msg_stream <- function(conn, ...) {
  stop("not written");
}
